<link rel='stylesheet' href='css/app.css'>
{{#partial "content"}}
    {{#if normal}}
        <table class="table text-center" style="opacity:0.1;">
            <h4 class="text-center"style="color: #fff;font-size: xx-large;font-weight: 600;">Animals in the safe zone(Not Endangered)</h4>
            <thead class="thead-dark">
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Health</th>
                <th scope="col">Age</th>
            </tr>
            </thead>
            <tbody style="color:#fff;">
            {{#each normal}}
                <tr>
                    <td>{{name}}</td>
                    <td>{{health}}</td>
                    <td>{{age}}</td>
                </tr>
            {{/each}}
            </tbody>
        </table>
    {{else}}
        <h5 class="text-center">No animals sighted .</h5>
    {{/if}}

{{/partial}}

{{> layout.hbs}}